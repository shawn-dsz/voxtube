# VoxTube Configuration

# Server port
PORT=3000

# Kokoro TTS server URL
KOKORO_URL=http://localhost:8880

# Cache directory for generated audio
CACHE_DIR=./cache

# How many days to keep cached audio
CACHE_TTL_DAYS=7

# Path to YouTube transcript CLI
# Install from: https://github.com/shawn-dsz/youtube-transcribe
YT_CLI_PATH=yt

# Maximum transcript length (characters)
MAX_TRANSCRIPT_LENGTH=100000

# Background cache cleanup interval (hours)
CLEANUP_INTERVAL_HOURS=12

# LLM provider: openai_compat (Kimmy/Kimi-style API) or claude_cli
LLM_PROVIDER=openai_compat

# LLM API key (required for openai_compat)
LLM_API_KEY=

# OpenAI-compatible base URL and model (Kimmy/Kimi example defaults)
LLM_BASE_URL=https://api.moonshot.ai/v1
LLM_MODEL=moonshot-v1-8k

# Request timeout for LLM calls (milliseconds)
LLM_TIMEOUT_MS=120000

# Path to Claude CLI (only used when LLM_PROVIDER=claude_cli)
CLAUDE_CLI_PATH=claude
